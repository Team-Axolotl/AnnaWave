<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:navigationBarColor">@color/black_overlay</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowSharedElementEnterTransition">@transition/content_transition</item>
        <item name="android:windowSharedElementExitTransition">@transition/content_transition</item>

        <item name="android:imageButtonStyle">@style/AppImageButtonStyle</item>
        <item name="imageButtonStyle">@style/AppImageButtonStyle</item>
    </style>

    <style name="AppImageButtonStyle" parent="android:Widget.ImageButton">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="ClickableTextView" parent="android:Widget.TextView">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="TextInputWhite">
        <item name="android:colorControlNormal">#ffffff</item>
        <item name="android:colorControlActivated">#ffffff</item>
        <item name="android:colorAccent">#ffffff</item>
    </style>

    <style name="EditTextTheme">
        <item name="colorControlNormal">#ffffff</item>
    </style>

    <style name="SplashActivityTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@color/blueSplash</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
