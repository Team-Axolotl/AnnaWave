<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/white</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowSharedElementEnterTransition">@transition/content_transition</item>
        <item name="android:windowSharedElementExitTransition">@transition/content_transition</item>

        <item name="imageButtonStyle">@style/AppTheme.ImageButtonStyle</item>
    </style>

    <style name="AppTheme.ImageButtonStyle" parent="android:Widget.ImageButton">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="ClickableTextView" parent="android:Widget.TextView">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
    </style>


    <style name="SplashActivityTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@color/blueSplash</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LoginTextInputEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:nextFocusDown">@id/input_password</item>
        <item name="android:paddingEnd">18dp</item>
        <item name="android:paddingStart">@dimen/spacingSmall</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@drawable/underline</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="LoginTextInputLayout">
        <item name="android:colorAccent">@color/white</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_marginStart">@dimen/spacingMid</item>
        <item name="android:layout_marginEnd">@dimen/spacingMid</item>
        <item name="layout_constraintWidth_max">400dp</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

</resources>
