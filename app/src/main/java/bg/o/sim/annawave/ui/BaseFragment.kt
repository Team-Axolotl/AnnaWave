package bg.o.sim.annawave.ui

import androidx.fragment.app.Fragment

/**
 * Root [Fragment], which is to be extended by *every* Fragment instance in the application.
 * Provides a place to implement any logic that is to be present in *all* Application fragments!
 */
abstract class BaseFragment : Fragment()